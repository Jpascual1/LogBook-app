This is a log book app to track the reps, sets, and weight of your lifts each session.

To run Frontend just input "npm run dev" while in client directory

To run Backend just input "node --env-file=config.env server.js"
